use ecs::ecs_world::ECSWorld;

pub struct DevConsolePlugin {
    // show: bool,
    //
    // background_rect_id: u64,
}

impl DevConsolePlugin {
    pub fn new(ecs: &mut ECSWorld) -> Self {
        Self {}
    }

    // pub update()
}
