use core::game_context::GameContext;
use ecs::ecs_world::ECSWorld;

pub struct OnFootInSpace {
    //
}

impl OnFootInSpace {
    pub fn support_current_situation(context: &GameContext, ecs: &ECSWorld) -> bool {
        let altitude = 
    }
}
